%h3.page-title
  = "#{t('.projects')} (#{@projects.total_count})"
.light
  = t('.you_can_browse_public_projects')
%hr
.clearfix
  .pull-left
    = form_tag public_projects_path, method: :get, class: 'form-inline form-tiny' do |f|
      .form-group
        = search_field_tag :search, params[:search], placeholder: t('.filter_by_name'), class: "form-control search-text-input input-mn-300", id: "projects_search"
      .form-group
        = submit_tag t('.search'), class: "btn btn-primary wide"

  .pull-right
    .dropdown.inline
      %a.dropdown-toggle.btn{href: '#', "data-toggle" => "dropdown"}
        %span.light #{t('.sort')}:
        - if @sort.present?
          = t(@sort.humanize.parameterize.underscore, scope: 'public.projects.index')
        - else
          = t('.name')
        %b.caret
      %ul.dropdown-menu
        %li
          = link_to public_projects_path(sort: nil) do
            = t('.name')
          = link_to public_projects_path(sort: 'newest') do
            = t('.newest')
          = link_to public_projects_path(sort: 'oldest') do
            = t('.oldest')
          = link_to public_projects_path(sort: 'recently_updated') do
            = t('.recently_updated')
          = link_to public_projects_path(sort: 'last_updated') do
            = t('.last_updated')

%hr
.public-projects
  %ul.bordered-list.top-list
    - @projects.each do |project|
      %li
        %h4
          = link_to project_path(project) do
            = project.name_with_namespace
          - if project.internal?
            %small.access-icon
              = internal_icon
              Internal
          .pull-right
            %pre.public-clone git clone #{project.http_url_to_repo}

        - if project.description.present?
          %p
            = project.description

        .repo-info
          - unless project.empty_repo?
            = link_to pluralize(project.repository.round_commit_count, 'commit'), project_commits_path(project, project.default_branch)
            &middot;
            = link_to pluralize(project.repository.branch_names.count, 'branch'), project_branches_path(project)
            &middot;
            = link_to pluralize(project.repository.tag_names.count, 'tag'), project_tags_path(project)
          - else
            %i.icon-warning-sign
            = t('.empty_repository')
    - unless @projects.present?
      .nothing-here-block= t('.no_public_projects')

  = paginate @projects, theme: "gitlab"
